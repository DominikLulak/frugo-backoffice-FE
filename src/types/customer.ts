export type Customer = {
    customerNumber: string;
    name: string;
    email: string;
    phoneNumber: string;
}

export type CustomerDetail = {
    customerNumber: string;
    name: string;
    country: string;
    address: string;
    executive: string;
    email: string;
    phoneNumber: string;
}